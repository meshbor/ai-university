# Legacy dashboard (архив)

Одностраничный dashboard до миграции на React (`apps/web`).

- **URL:** https://meshbor.github.io/ai-university/legacy/index.html
- **Заменён:** v2 SPA из `apps/web/dist` (деплой через GitHub Actions)
- **Данные:** те же ключи `localStorage` (`aiuni_pipboy_v1`, `aiuni_profile_v1`, …) — совместимы с v2

Не правим для новых фич; только критичные фиксы контента курсов.
